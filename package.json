{
  "name": "automate_now_cy",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress:open": "cypress open",
    "cypress:run:chrome": "cypress run --browser chrome",
    "cypress:run:formTests": "cypress run --spec cypress/e2e/form_specs/*.spec.cy.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^14.0.3"
  },
  "devDependencies": {
    "cypress-mochawesome-reporter": "^3.8.2"
  }
}
